# Next session

> **Parallel agents:** Elke agent werkt op één pagina. Update **alleen jouw sectie** hieronder — laat andere secties staan.

---

## Algemeen
Vercel project aanmaken + DNS DaddyGo koppelen.

## Homepage (`/`)
OG-image vervangen (`website-template-OG.webp`).

## Aanpak (`/aanpak`)
Gsm-placeholders vervangen zodra Figma exports beschikbaar zijn.

## Pricing (`/pricing`)
Vercel env vars zetten (Resend + Turnstile + `contact@iknowright.be`) en live test na deploy.

## Contact (`/contact`)
_(klaar voor v1 — optioneel: telefoonnummer footer vs pagina gelijk trekken)_

## Cases (`/cases`)
Eerste echte case toevoegen: kopieer `caseDetailTemplate`, zet eigen `slug`, update `caseGridItems[].slug` naar die slug.
